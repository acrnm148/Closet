package closet.domain.repository;

import closet.domain.display.entity._Display;
import org.springframework.data.jpa.repository.JpaRepository;

public interface _DisplayRepository extends JpaRepository<_Display, Long> {
}
